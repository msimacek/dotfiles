# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1
set_from_resource $primary I3.primary DP-3-2

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
#font pango:DejaVu Sans Mono 9.5
font xft:DejaVu Sans Mono 9.5

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvt256c-ml -ls -cd "$(i3cwd.py)"
bindsym $mod+Shift+Return exec zsh -c 'cd "$(i3cwd.py)" && qgit'

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+d exec --no-startup-id dmenu_run

bindsym Menu scratchpad show
bindsym Print scratchpad show
bindsym $mod+Menu move scratchpad
bindsym $mod+Print move scratchpad

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+g split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

#minus
bind $mod+20 workspace back_and_forth

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
bind $mod+29 focus child

# switch to workspace
bind $mod+10 workspace number 1
bind $mod+11 workspace number 2
bind $mod+12 workspace number 3
bind $mod+13 workspace number 4
bind $mod+14 workspace number 5
bind $mod+15 workspace number 6
bind $mod+16 workspace number 7
bind $mod+17 workspace number 8
bind $mod+18 workspace number 9
bind $mod+19 workspace number 10
bind $mod+21 workspace i
bind $mod+22 workspace e
bind $mod+51 workspace r

# move focused container to workspace
bind $mod+Shift+10 move container to workspace number 1
bind $mod+Shift+11 move container to workspace number 2
bind $mod+Shift+12 move container to workspace number 3
bind $mod+Shift+13 move container to workspace number 4
bind $mod+Shift+14 move container to workspace number 5
bind $mod+Shift+15 move container to workspace number 6
bind $mod+Shift+16 move container to workspace number 7
bind $mod+Shift+17 move container to workspace number 8
bind $mod+Shift+18 move container to workspace number 9
bind $mod+Shift+19 move container to workspace number 10
bind $mod+Shift+21 move container to workspace i
bind $mod+Shift+22 move container to workspace e
bind $mod+Shift+51 move container to workspace r

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#bindsym $mod+r mode "resize"

bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev

bind $mod+48 exec i3-input -F '[con_mark=%s] focus' -P 'goto: '
bindsym $mod+m exec i3-input -F 'mark %s' -P 'mark: '
bind $mod+Shift+48 exec i3-input -F '[con_mark=%s] move workspace current' -P 'move here: '

bind $mod+61 exec i3-input -P 'exec: '

bindsym $mod+Shift+m exec i3-input -F 'rename workspace to %s' -P 'rename workspace: '
bindsym $mod+Shift+n exec i3-input -F 'move to workspace %s' -P 'move to workspace: '
bindsym $mod+n exec i3-input -F 'workspace %s' -P 'workspace: '

# lock screen
bindsym $mod+Escape exec xset s activate

# Volume keys and brightness
bindsym XF86AudioLowerVolume exec --no-startup-id _volume_dn.sh
bindsym XF86AudioRaiseVolume exec --no-startup-id _volume_up.sh
bindsym XF86AudioMute exec --no-startup-id _mute.sh
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 10
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 10
bind 156 exec --no-startup-id xbacklight -set 75
bind $mod+Shift+156 exec --no-startup-id xbacklight -set 0
bind $mod+156 exec --no-startup-id xbacklight -set 100

bindsym $mod+x move workspace to output right
bind $mod+52 move workspace to output left

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        position top
        status_command i3status
        tray_output primary
}

client.focused          #285577 #285577 #ffffff #2e9ef4
client.focused_inactive #5f676a #5f676a #ffffff #484e50
client.unfocused        #222222 #222222 #888888 #292d2e
client.urgent           #900000 #900000 #ffffff #900000

workspace 1 output $primary
workspace 2 output $primary
workspace 3 output $primary
workspace 4 output $primary
workspace 5 output $primary
workspace 6 output $primary
workspace 7 output $primary
workspace 8 output $primary
workspace 8 output $primary
workspace 9 output $primary
workspace 10 output $primary
workspace e output DP-3-3
workspace r output DP-3-3
workspace m output DP-3-3

bindsym $mod+a exec --no-startup-id randrctl auto
bindsym $mod+Shift+a exec --no-startup-id xrandr --auto

# screenshot
bindsym --release $mod+Shift+Print exec scrot -s
bindsym --release $alt+Shift+Print exec scrot

for_window [class="^.*"] border normal
#for_window [instance="sun-awt-X11-XDialogPeer"] floating disable
for_window [title="Emoji Choice"] floating enable

bindsym $mod+Pause exec --no-startup-id systemctl suspend
bindsym $mod+Shift+Pause exec --no-startup-id systemctl hibernate

# keyboard layout keybindings
# get current engine with ibus engine
bindsym $mod+F1 exec --no-startup-id ibus engine xkb:us::eng
bindsym $mod+F2 exec --no-startup-id ibus engine kkc
bindsym $mod+F3 exec --no-startup-id ibus engine xkb:cz:qwerty:cze
bindsym $mod+F4 exec --no-startup-id ibus engine xkb:fi::fin
bindsym $mod+F5 exec --no-startup-id ibus engine libpinyin

#exec --no-startup-id xsetroot -solid '#004422'
# executes compton and sets background
exec_always --no-startup-id randrctl auto
exec --no-startup-id _udev-randrctl.py
exec --no-startup-id gnome-keyring-daemon
exec --no-startup-id /usr/libexec/mate-settings-daemon
exec --no-startup-id nm-applet
exec --no-startup-id /usr/libexec/polkit-gnome-authentication-agent-1
exec --no-startup-id clipit
exec --no-startup-id xss-lock -- i3lock -nc 000000
exec --no-startup-id xset s off
exec --no-startup-id xset -dpms
exec --no-startup-id xkbset -a
exec --no-startup-id ibus-daemon --xim -d
exec --no-startup-id dropbox start
exec --no-startup-id pasystray
